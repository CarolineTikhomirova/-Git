# -Git
Проект помощник
Это шпаргалка с основными командами Git’а.


------------------------------------------------------------------------

## 📚 Глоссарий

  -----------------------------------------------------------------------
  Термин                          Описание
  ------------------------------- ---------------------------------------
  **Репозиторий (repository)**    Хранилище вашего проекта. Включает все
                                  файлы и историю изменений.

  **Клонирование (clone)**        Создание локальной копии существующего
                                  репозитория.

  **Коммит (commit)**             Зафиксированное состояние проекта в
                                  определённый момент времени.

  **Ветка (branch)**              Независимая линия разработки. Позволяет
                                  работать над изменениями, не влияя на
                                  основную ветку.

  **Слияние (merge)**             Интеграция изменений из одной ветки в
                                  другую.

  **Пул-реквест (pull request)**  Запрос на внесение изменений в основную
                                  ветку проекта.

  **Пул (pull)**                  Получение изменений из удалённого
                                  репозитория.

  **Пуш (push)**                  Отправка изменений в удалённый
                                  репозиторий.

  **Индекс (index)**              Область, в которую добавляются
                                  изменения перед коммитом.

  **Фетч (fetch)**                Получение изменений из удалённого
                                  репозитория без интеграции.

  **Тег (tag)**                   Метка для определённых коммитов
                                  (например, для релиза).

  **Стэш (stash)**                Временное сохранение незакоммиченных
                                  изменений.

  **Конфликт слияния (merge       Противоречие при объединении веток.
  conflict)**                     

  **Форк (fork)**                 Копия чужого репозитория в вашем
                                  GitHub-аккаунте.

  **Ревью (review)**              Проверка и обсуждение кода другими
                                  разработчиками.
  -----------------------------------------------------------------------

------------------------------------------------------------------------

## ⚙️ Список базовых команд

  ----------------------------------------------------------------------------------------------
  Команда                                                Описание
  ------------------------------------------------------ ---------------------------------------
  `git init`                                             Инициализировать новый репозиторий.

  `git clone <url>`                                      Клонировать репозиторий.

  `git status`                                           Проверить состояние репозитория.

  `git add <file>`                                       Добавить файл в индекс.

  `git commit -m "message"`                              Сделать коммит с сообщением.

  `git push [remote] [branch]`                           Отправить изменения в удалённый
                                                         репозиторий.

  `git pull [remote] [branch]`                           Получить и объединить изменения из
                                                         удалённого репозитория.

  `git branch`                                           Показать список веток.

  `git branch <name>`                                    Создать новую ветку.

  `git checkout <branch>`                                Переключиться на ветку.

  `git checkout -b <branch>`                             Создать и переключиться на ветку.

  `git merge <branch>`                                   Объединить ветку с текущей.

  `git diff`                                             Показать различия между изменениями.

  `git log`                                              Просмотреть историю коммитов.

  `git stash`                                            Сохранить незакоммиченные изменения.

  `git stash pop`                                        Применить сохранённые изменения.

  `git fetch`                                            Получить изменения без их объединения.

  `git remote add <name> <url>`                          Добавить удалённый репозиторий.

  `git tag`                                              Просмотреть список тегов.

  `git tag <name>`                                       Создать тег.

  `git config --global user.name "Имя"`                  Настроить имя пользователя.

  `git config --global user.email "email@example.com"`   Настроить email.

  `git config --list`                                    Показать настройки.
  ----------------------------------------------------------------------------------------------

------------------------------------------------------------------------

## 🔄 Pull Request (Пул-реквест)

1.  **Создайте новую ветку:**

    ``` bash
    git checkout -b <имя_ветки>
    ```

2.  **Внесите изменения** в код проекта.

3.  **Добавьте и закоммитьте изменения:**

    ``` bash
    git add .
    git commit -m "Описание изменений"
    ```

4.  **Отправьте ветку на GitHub:**

    ``` bash
    git push origin <имя_ветки>
    ```

5.  **Создайте Pull Request** через GitHub → *Compare & pull request →
    Create pull request*.

После этого ревьюер проверит код и при необходимости предложит правки.

------------------------------------------------------------------------

## 🧾 .gitignore

Файл `.gitignore` используется для исключения файлов и папок из
отслеживания.

**Примеры:**

    logs/
    config.env
    *.log
    /temp/*
    !.gitignore
    !*.md

> 🔗 Создайте шаблон на
> [gitignore.io](https://www.toptal.com/developers/gitignore)

------------------------------------------------------------------------

## ✍️ Правила написания коммитов

✅ **Хорошие примеры:** - Добавлен алгоритм автосохранения формы.\
- Исправлены ошибки в код-стайле ViewController.\
- Добавлена история измерений на главный экран.

🚫 **Плохие примеры:** - Исправлены ошибки.\
- Fix.\
- Правка замечаний.

**Рекомендации:** - Сообщение должно быть конкретным и информативным.\
- Используйте обезличенные глаголы: *Добавлен*, *Исправлена*,
*Удалено*.\
- Первая строка ≤ 50 символов.\
- Начинайте с заглавной буквы.

------------------------------------------------------------------------

## 🧱 Markdown: язык разметки

Markdown используется для форматирования текста на GitHub.

### Заголовки

``` md
# Заголовок 1
## Заголовок 2
### Заголовок 3
```

### Выделение текста

-   *Курсив* → `_текст_` или `*текст*`
-   **Жирный** → `__текст__` или `**текст**`
-   ***Жирный курсив*** → `___текст___`

### Списки

``` md
- Пункт 1
- Пункт 2

1. Первый пункт
2. Второй пункт
```

### Ссылки

``` md
[Practicum](https://practicum.yandex.ru)
```

### Изображения

``` md
![Описание](url_изображения)
```

### Код

``` md
`inline-код`

```swift
print("Hello world!")
```


    ### Цитаты
    ```md
    > Это цитата.

### Разделители

``` md
---
***
___
```

------------------------------------------------------------------------
